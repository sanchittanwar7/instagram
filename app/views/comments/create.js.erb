$("#comments_<%= @post.id %>").prepend("<%= escape_javascript (render 'comment', comment: @comment, post: @post) %>");
$(".comment-form").val("");
$("#comments-count_<%= @post.id %>").html("<%= @count %> comments");